<div class="border border-indigo-100 rounded-lg p-4 bg-indigo-50 space-y-4">
  <p class="text-xs text-indigo-600 font-medium"><%= suggestions.count %> sugestão(ões) gerada(s)</p>
  <% suggestions.each do |suggestion| %>
    <%= render "playbook_suggestions/suggestion", suggestion: suggestion, playbook: playbook %>
  <% end %>
</div>
