# Rails
RAILS_ENV=production
RAILS_MASTER_KEY=
SECRET_KEY_BASE=
VIRALSPY_HOST=viralspy.curt.com.br

# Database
POSTGRES_USER=viralspy
POSTGRES_PASSWORD=
POSTGRES_DB=viralspy_production

# Redis
REDIS_URL=redis://redis:6379/0
SIDEKIQ_REDIS_URL=redis://redis:6379/1

# Encryption (vai pra credentials em produção)
ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=
ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=
ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=

# Scraping — chave da plataforma
APIFY_API_TOKEN=
SCRAPING_PROVIDER=apify
SCRAPING_POSTS_PER_ANALYSIS=30

# =====================================================================
# LLM / Transcription
# ---------------------------------------------------------------------
# Em produção NÃO EXISTEM chaves de LLM/Transcription em ENV. Cada usuário
# configura suas próprias chaves via Settings → API Keys, armazenadas
# encriptadas na tabela `api_credentials`. O código nunca lê
# ENV["OPENAI_API_KEY"], ENV["ANTHROPIC_API_KEY"] ou ENV["ASSEMBLYAI_API_KEY"]
# em produção — sempre via `account.api_credential_for(provider)`.
# =====================================================================

# Feature flags
ENABLE_SIGNUP=true

# Backup (Cloudflare R2 — S3-compatible)
# Endpoint: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# Obter ACCOUNT_ID em: Cloudflare dashboard → R2 → sidebar
# Gerar API token em: R2 → Manage R2 API Tokens → Create API Token
# Region R2 é literalmente a string "auto" (não é região geográfica)
BACKUP_S3_ENDPOINT=
BACKUP_S3_ACCESS_KEY=
BACKUP_S3_SECRET_KEY=
BACKUP_S3_BUCKET=viralspy-backups
BACKUP_S3_REGION=auto

# DATABASE_URL — construído a partir das vars POSTGRES_*
DATABASE_URL=
