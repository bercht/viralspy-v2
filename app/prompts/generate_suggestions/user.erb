Perfil analisado: @<%= handle %> (<%= followers %> seguidores).

Métricas do concorrente:
<%= profile_metrics.to_json %>

Insights extraídos da análise:
<%= insights.to_json %>

Gere <%= target_count %> sugestões de conteúdo mantendo o mix <%= mix_label %>. Retorne apenas o JSON conforme especificado.
