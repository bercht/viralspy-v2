<div class="max-w-2xl mx-auto space-y-6">
  <div>
    <h1 class="text-display text-text-body">Editar Perfil</h1>
    <p class="text-body-sm text-text-muted mt-1">@<%= @own_profile.instagram_handle %></p>
  </div>
  <div class="rounded-card border border-border-subtle bg-surface-canvas p-6 shadow-card">
    <%= render "form", own_profile: @own_profile %>
  </div>
</div>
